---
# This is an example playbook to execute Ansible tests.

- name: Verify
  hosts: all
  gather_facts: false
  tasks:
    - name: Load variables
      include_vars:
        file: ../../defaults/main.yml
